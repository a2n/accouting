;統計
=filter(每日記帳!d2:d9,
	每日記帳!B2:B9=C3,
	每日記帳!A2:A9 >= A3,
	每日記帳!A2:A9 <= B3)

;數量統計
=sum(
    if(isna(
	;若沒有符合的結果，回傳 0。
	filter( 每日記帳!d2:d9,
		每日記帳!B2:B9=C3,
		每日記帳!A2:A9 >= A3,
		每日記帳!A2:A9 <= B3)
    ),0)
)

;合計統計
=sum(
    if(isna(
	;若沒有符合的結果，回傳 0。
	filter( 每日記帳!e2:e9,
		每日記帳!B2:B9=C3,
		每日記帳!A2:A9 >= A3,
		每日記帳!A2:A9 <= B3)
    ),0)
)
